contentOrigin:
  # Define the source of packages - path to your repo file
  repofiles:
    - ./redhat.repo

packages:
  # List of RPM packages to resolve
  - openshift-clients

arches:
  # Architectures for which packages should be resolved
  - aarch64
  - x86_64

context:
  image: registry.redhat.io/ubi9/ubi-minimal:latest
