apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-web-app-helm.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "go-web-app-helm.name" . }}
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}